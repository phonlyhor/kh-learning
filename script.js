const fs = require('fs'); const path = require('path'); const dir = 'd:/KH-LEARNING/KHMER-LEARNING/src/data'; const files = fs.readdirSync(dir).filter(f => f.endsWith('.js')); files.forEach(file => { const content = fs.readFileSync(path.join(dir, file), 'utf8'); const regex = /classId:\s*(\d+).*?title:\s*['"]([^'"]+)['"].*?pdf:\s*['"]([^'"]+)['"]/gs; let match; while(match = regex.exec(content)) { console.log(file, 'ClassId:', match[1], 'Title:', match[2], 'PDF:', match[3]); } });
